# Секреты и внутренние заметки — НИКОГДА не публиковать
keys.md
CLAUDE.md

# Зависимости и тяжёлые исходники (для деплоя не нужны)
node_modules/
video/

# Служебное
.DS_Store
*.log
package-lock.json

# Backend secrets (bot token, chat ids)
server/config.json
